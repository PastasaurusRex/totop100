# Optional locally — without these, visited state is in-memory (dev only).
# On Vercel these are injected automatically by the Upstash marketplace integration.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
